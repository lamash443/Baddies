<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Escort Videos of Kenya Escorts and Discreet Call Girls - Baddies-club</title>
  <meta name="description" content="Watch escort videos of Kenya escorts and discreet call girls on Baddies-club.">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; }
    html, body { margin:0; padding:0; font-family:"Outfit",sans-serif; background:#0d0d0d; color:#fff; min-height:100vh; }

    /* NAVBAR */
    .nr-navbar {
      background: linear-gradient(160deg, #0d0d0d 0%, #1a0f00 50%, #0d0d0d 100%) !important;
      border-bottom: 2px solid rgba(255,140,0,0.6);
      box-shadow: inset 0 -2px 30px rgba(255,140,0,0.06);
      padding-top:1.25rem !important;
      padding-bottom:1.25rem !important;
    }
    .nr-navbar .nav-link {
      font-weight:500; font-size:0.82rem;
      color:rgba(255,140,0,0.92) !important;
      text-decoration:none !important;
      display:inline-block !important;
      position:relative !important;
      padding-bottom:3px !important;
      box-shadow:none !important;
      transition:color 0.3s ease !important;
    }
    .nr-navbar .nav-link::after {
      content:"" !important; position:absolute !important;
      left:0 !important; bottom:0 !important;
      width:100% !important; height:1.5px !important;
      background:linear-gradient(90deg,rgba(255,255,255,0) 0%,rgba(255,255,255,0.9) 50%,rgba(255,255,255,0) 100%) !important;
      border-radius:2px !important;
      transform:scaleX(0) !important; transform-origin:center !important;
      transition:transform 0.35s cubic-bezier(0.4,0,0.2,1) !important;
    }
    .nr-navbar .nav-link.active,
    .nr-navbar .nav-link:hover,
    .nr-navbar .nav-link:focus {
      color:#ffffff !important; text-decoration:none !important; box-shadow:none !important;
    }
    .nr-navbar .nav-link.active::after,
    .nr-navbar .nav-link:hover::after,
    .nr-navbar .nav-link:focus::after { transform:scaleX(1) !important; }

    /* BREADCRUMB */
    .vd-breadcrumb-bar {
      background:linear-gradient(160deg,#0d0d0d 0%,#1a0f00 50%,#0d0d0d 100%);
      border-bottom:1px solid rgba(255,140,0,0.18);
      padding:0.65rem 0;
    }
    .vd-breadcrumb {
      display:flex; align-items:center; gap:0.4rem;
      font-size:0.78rem; font-weight:500; color:rgba(255,255,255,0.45);
      list-style:none; margin:0; padding:0; flex-wrap:wrap;
    }
    .vd-breadcrumb li { display:flex; align-items:center; gap:0.4rem; }
    .vd-breadcrumb a { color:rgba(255,140,0,0.85); text-decoration:none; transition:color 0.2s; }
    .vd-breadcrumb a:hover { color:#fff; }
    .vd-breadcrumb .sep { color:rgba(255,255,255,0.2); }
    .vd-breadcrumb .current { color:rgba(255,255,255,0.6); }

    /* PAGE HEADER */
    .vd-page-header { padding:3.5rem 0 2rem; border-bottom:1px solid rgba(255,140,0,0.12); margin-bottom:2.5rem; }
    .vd-page-title { font-size:clamp(1.8rem,4vw,2.8rem); font-weight:900; letter-spacing:-0.02em; line-height:1.1; margin-bottom:0.5rem; }
    .vd-page-title span { background:linear-gradient(135deg,#ff8c00,#ffb347); -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text; }
    .vd-page-sub { font-size:0.7rem; letter-spacing:0.12em; text-transform:uppercase; color:rgba(255,140,0,0.7); font-weight:600; margin-bottom:0.5rem; }
    .vd-page-divider { height:2px; width:60px; margin-top:1rem; background:linear-gradient(90deg,#ff8c00,#ffb347,transparent); border-radius:2px; }

    /* VIDEO GRID */
    .vd-grid { display:grid; grid-template-columns:repeat(auto-fill, minmax(280px, 1fr)); gap:1.5rem; padding-bottom:4rem; }

    /* VIDEO CARD */
    .vd-card {
      background:#111; border:1px solid rgba(255,140,0,0.12);
      border-radius:14px; overflow:hidden; cursor:pointer;
      transition:transform 0.28s ease, box-shadow 0.28s ease, border-color 0.28s ease;
      display: flex; flex-direction: column;
    }
    .vd-card:hover { transform:translateY(-6px) scale(1.015); box-shadow:0 16px 48px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,140,0,0.35); border-color:rgba(255,140,0,0.4); }
    .vd-card:focus-visible { outline:2px solid rgba(255,140,0,0.7); outline-offset:2px; }

    /* Thumbnail */
    .vd-card__thumb { position:relative; width:100%; padding-top:56.25%; background:#1a1a1a; overflow:hidden; }
    .vd-card__thumb-inner { position:absolute; inset:0; display:flex; align-items:center; justify-content:center; }

    /* Play overlay */
    .vd-card__play { position:absolute; inset:0; display:flex; align-items:center; justify-content:center; background:rgba(0,0,0,0.35); transition:background 0.28s ease; z-index:1; }
    .vd-card:hover .vd-card__play { background:rgba(0,0,0,0.55); }
    .vd-card__play-icon {
      width:54px; height:54px; background:rgba(255,140,0,0.92); border-radius:50%;
      display:flex; align-items:center; justify-content:center;
      box-shadow:0 4px 20px rgba(255,140,0,0.5);
      transition:transform 0.28s cubic-bezier(0.34,1.56,0.64,1), box-shadow 0.28s ease;
    }
    .vd-card:hover .vd-card__play-icon { transform:scale(1.15); box-shadow:0 8px 32px rgba(255,140,0,0.7); }
    .vd-card__play-icon svg { margin-left:4px; }

    /* Duration badge */
    .vd-card__duration {
      position:absolute; bottom:0.55rem; right:0.55rem; z-index:2;
      background:rgba(0,0,0,0.78); color:#fff;
      font-size:0.7rem; font-weight:600; letter-spacing:0.04em;
      padding:0.18rem 0.5rem; border-radius:4px; backdrop-filter:blur(4px);
    }

    /* Card info */
    .vd-card__info { padding:0.9rem 1rem 1rem; display:flex; flex-direction:column; flex: 1; }
    .vd-card__name { font-size:0.98rem; font-weight:700; color:#fff; margin-bottom:0.3rem; line-height:1.3; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .vd-card__meta { display:flex; align-items:center; gap:0.7rem; font-size:0.72rem; color:rgba(255,255,255,0.38); font-weight:400; }
    .vd-card__meta span { display:flex; align-items:center; gap:0.25rem; }
    .vd-card__tag { display:inline-block; margin-top:0.6rem; font-size:0.65rem; font-weight:600; letter-spacing:0.07em; text-transform:uppercase; color:rgba(255,140,0,0.8); background:rgba(255,140,0,0.08); border:1px solid rgba(255,140,0,0.2); border-radius:4px; padding:0.15rem 0.55rem; }
    
    /* View Profile Button Override */
    .vd-btn-primary,
    .vd-btn-primary:visited {
      background-color: #ff8c00 !important;
      border: 1px solid #ff8c00 !important;
      color: #000 !important;
      font-weight: 700 !important;
      transition: all 0.3s ease !important;
    }
    .vd-btn-primary:hover,
    .vd-btn-primary:focus {
      background-color: #000 !important;
      border-color: #ff8c00 !important;
      color: #ff8c00 !important;
      box-shadow: 0 0 18px rgba(255,140,0,0.5) !important;
    }

    /* LIGHTBOX */
    .vd-lightbox { display:none; position:fixed; inset:0; z-index:1060; background:rgba(0,0,0,0.92); backdrop-filter:blur(10px); -webkit-backdrop-filter:blur(10px); align-items:center; justify-content:center; padding:1rem; }
    .vd-lightbox.open { display:flex; animation:lbFadeIn 0.2s ease both; }
    @keyframes lbFadeIn { from{opacity:0;} to{opacity:1;} }
    .vd-lightbox__inner { position:relative; width:100%; max-width:900px; background:#0d0d0d; border:1px solid rgba(255,140,0,0.25); border-radius:18px; overflow:hidden; box-shadow:0 24px 80px rgba(0,0,0,0.8); animation:lbIn 0.3s cubic-bezier(0.34,1.2,0.64,1) both; }
    @keyframes lbIn { from{opacity:0;transform:scale(0.88);} to{opacity:1;transform:scale(1);} }
    .vd-lightbox__video-wrap { position:relative; padding-top:56.25%; background:#000; }
    .vd-lightbox__video-wrap video { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; }
    .vd-lightbox__bar { padding:1rem 1.25rem; display:flex; align-items:flex-start; justify-content:space-between; gap:1rem; border-top:1px solid rgba(255,140,0,0.1); }
    .vd-lightbox__title { font-size:1.1rem; font-weight:700; color:#fff; margin-bottom:0.25rem; }
    .vd-lightbox__meta { font-size:0.75rem; color:rgba(255,255,255,0.4); display:flex; gap:0.8rem; }
    .vd-lightbox__close { flex-shrink:0; width:36px; height:36px; background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.12); border-radius:50%; display:flex; align-items:center; justify-content:center; cursor:pointer; color:#fff; transition:background 0.2s ease, border-color 0.2s ease; }
    .vd-lightbox__close:hover { background:rgba(255,140,0,0.2); border-color:rgba(255,140,0,0.5); }

    /* AUTH MODAL */
    .auth-input { background-color:#1a1a1a; border:1px solid #444; color:white; padding:12px; border-radius:8px; width:100%; }
    .auth-input:focus { background-color:#1a1a1a; border-color:orange; color:white; box-shadow:0 0 0 0.25rem rgba(255,165,0,0.25); outline:none; }
    .auth-btn { background-color:orange; color:black; font-weight:bold; padding:12px; border-radius:8px; border:1px solid orange; transition:0.3s; text-transform:uppercase; letter-spacing:1px; width:100%; }
    .auth-btn:hover { background-color:#e69500; color:black; box-shadow:0 4px 15px rgba(255,165,0,0.4); }
    #authTabs .nav-link.active { background-color:orange !important; color:black !important; }
    #authTabs .nav-link:hover:not(.active) { background-color:rgba(255,165,0,0.2); }

    /* FOOTER */
    .nr-footer { background:linear-gradient(160deg,#0d0d0d 0%,#1a0f00 50%,#0d0d0d 100%); color:rgba(255,255,255,0.78); border-top:2px solid rgba(255,140,0,0.6); box-shadow:inset 0 2px 30px rgba(255,140,0,0.06); }
    .nr-footer__brand,.nr-footer__title{color:#fff;}
    .nr-footer a { color:rgba(255,140,0,0.92); text-decoration:none; display:inline-block; position:relative; padding-bottom:3px; transition:color 0.3s ease; }
    .nr-footer a::after { content:""; position:absolute; left:0; bottom:0; width:100%; height:1.5px; background:linear-gradient(90deg,rgba(255,255,255,0) 0%,rgba(255,255,255,0.9) 50%,rgba(255,255,255,0) 100%); border-radius:2px; transform:scaleX(0); transform-origin:center; transition:transform 0.35s cubic-bezier(0.4,0,0.2,1); }
    .nr-footer a:hover,.nr-footer a:focus{color:#fff;text-decoration:none;}
    .nr-footer a:hover::after,.nr-footer a:focus::after{transform:scaleX(1);}
    .nr-footer__logo-text { font-size:1.6rem; font-weight:800; letter-spacing:-0.5px; line-height:1; background:linear-gradient(135deg,#ff8c00,#ffb347); -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text; display:inline-block; }
    hr.border-light-subtle{border-color:rgba(255,255,255,0.1) !important;}

    @keyframes fadeInUp { from{opacity:0;transform:translateY(24px);} to{opacity:1;transform:translateY(0);} }
    .fade-in { animation:fadeInUp 0.55s ease both; }

    /* ── LIGHT THEME OVERRIDES ── */
    [data-bs-theme="light"] body { background: #f4f6f9; color: #111; }
    [data-bs-theme="light"] .vd-breadcrumb-bar { background: #ffffff; border-bottom-color: rgba(0,0,0,0.08); }
    [data-bs-theme="light"] .vd-breadcrumb .sep { color: rgba(0,0,0,0.3); }
    [data-bs-theme="light"] .vd-breadcrumb .current { color: rgba(0,0,0,0.6); }
    
    [data-bs-theme="light"] .vd-page-header { border-bottom-color: rgba(0,0,0,0.08); }
    [data-bs-theme="light"] .vd-page-title { color: #111; }
    
    [data-bs-theme="light"] .vd-card { background: #ffffff; border-color: rgba(0,0,0,0.08); box-shadow: 0 4px 15px rgba(0,0,0,0.02); }
    [data-bs-theme="light"] .vd-card:hover { transform:translateY(-4px); box-shadow: 0 12px 30px rgba(0,0,0,0.06); border-color: rgba(255,140,0,0.35); }
    [data-bs-theme="light"] .vd-card__thumb-inner { background: #f8f9fa !important; }
    [data-bs-theme="light"] .vd-card__name { color: #111; }
    [data-bs-theme="light"] .vd-card__meta { color: rgba(0,0,0,0.55); }
    [data-bs-theme="light"] .vd-card__tag { background: rgba(255,140,0,0.05); color: #111; border-color: rgba(255,140,0,0.25); }
    [data-bs-theme="light"] .vd-card__duration { background: rgba(255,255,255,0.9); color: #111; }
  </style>
</head>
<body>

<!-- NAVBAR -->
<x-navbar :hideSearch="true" />




<!-- PAGE HEADER -->
<div class="vd-page-header">
  <div class="container">
    <p class="vd-page-sub">Videos</p>
    <h1 class="vd-page-title">Escort Videos of Kenya Escorts<br><span>and Discreet Call Girls</span></h1>
    <div class="vd-page-divider"></div>
  </div>
</div>

<!-- VIDEO GRID -->
<main class="container pb-5">
  <div class="vd-grid" id="vd-grid">

    @foreach($videos as $index => $video)
    <div class="vd-card fade-in" style="animation-delay:{{ min($index * 0.09, 1) }}s;"
      onclick="openLightbox('{{ $video->id }}','{{ asset('storage/' . $video->path) }}','{{ addslashes($video->user->name) }}','{{ $video->views }} views','{{ $video->created_at->diffForHumans() }}', this)"
      role="button" tabindex="0" aria-label="Play {{ $video->user->name }} video"
      onkeydown="if(event.key==='Enter'||event.key===' ')openLightbox('{{ $video->id }}','{{ asset('storage/' . $video->path) }}','{{ addslashes($video->user->name) }}','{{ $video->views }} views','{{ $video->created_at->diffForHumans() }}', this)">
      <div class="vd-card__thumb">
        <div class="vd-card__thumb-inner" style="background:#0d0d0d;">
          <video src="{{ asset('storage/' . $video->path) }}#t=0.1" preload="metadata" muted playsinline style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover;"></video>
        </div>
        <div class="vd-card__play">
          <div class="vd-card__play-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="#000"><path d="M8 5v14l11-7z"/></svg>
          </div>
        </div>
        <span class="vd-card__duration">Video</span>
      </div>
      <div class="vd-card__info">
        <div class="vd-card__name">{{ $video->user->name }}</div>
        <div class="vd-card__meta">
          <span class="vd-views-count">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="me-1"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>{{ number_format($video->views) }} views
          </span>
          <span>
            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
            {{ $video->created_at->diffForHumans() }}
          </span>
        </div>
        <div class="mt-2 mb-1">
          <span class="vd-card__tag mt-0">{{ $video->user->county ?? 'Kenya' }}</span>
        </div>
        <div class="mt-auto pt-3">
          <a href="{{ route('profile.view', $video->user->id) }}" class="btn vd-btn-primary w-100" style="font-size:0.9rem;" onclick="event.stopPropagation()">View Profile</a>
        </div>
      </div>
    </div>
    @endforeach

  </div>
</main>

<!-- LIGHTBOX -->
<div class="vd-lightbox" id="vd-lightbox" role="dialog" aria-modal="true" aria-label="Video player" onclick="closeLightboxOnBackdrop(event)">
  <div class="vd-lightbox__inner" id="vd-lightbox-inner">
    <div class="vd-lightbox__video-wrap">
      <video id="vd-lightbox-video" controls autoplay playsinline>
        Your browser does not support the video tag.
      </video>
    </div>
    <div class="vd-lightbox__bar">
      <div>
        <div class="vd-lightbox__title" id="vd-lb-title">—</div>
        <div class="vd-lightbox__meta">
          <span id="vd-lb-views">—</span>
          <span id="vd-lb-date">—</span>
        </div>
      </div>
      <button class="vd-lightbox__close" onclick="closeLightbox()" aria-label="Close video">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
      </button>
    </div>
  </div>
</div>

<!-- AUTH MODAL -->
<div class="modal fade" id="authModal" tabindex="-1" aria-labelledby="authModalLabel" aria-hidden="true" style="backdrop-filter:blur(5px);">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content" style="background-color:#000;border:1px solid #333;border-radius:12px;box-shadow:0 10px 30px rgba(0,0,0,0.5);">
      <div class="modal-header border-bottom-0 pb-0 mt-2 mx-2">
        <div class="w-100 d-flex justify-content-between align-items-center">
          <h5 class="modal-title fw-bold fs-3" id="authModalLabel" style="color:orange;text-transform:uppercase;letter-spacing:1px;">Kenyan Baddies Club</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
      </div>
      <div class="modal-body pt-0 px-4 pb-4">
        <ul class="nav nav-pills nav-justified mb-4 mt-4" id="authTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active rounded-pill fw-bold" id="tab-login" data-bs-toggle="pill" data-bs-target="#content-login" type="button" role="tab" style="color:white;border:1px solid orange;">Login</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link rounded-pill fw-bold ms-2" id="tab-register" data-bs-toggle="pill" data-bs-target="#content-register" type="button" role="tab" style="color:white;border:1px solid orange;">Sign Up</button>
          </li>
        </ul>
        <div class="tab-content">
          <!-- LOGIN -->
          <div class="tab-pane fade show active" id="content-login" role="tabpanel">
            <div class="text-center mb-4">
              <h4 class="text-light fw-bold">Member Log In</h4>
              <p class="text-secondary small fw-bold">Access your Baddies Club account</p>
            </div>
            <form method="POST" action="{{ route('login') }}">
              @csrf
              <div class="mb-3">
                <label class="form-label text-light small fw-bold">Email Address</label>
                <input type="email" name="email" class="auth-input" required autocomplete="email" value="{{ old('email') }}" placeholder="your@email.com">
                @error('email')<div class="text-danger small mt-1">{{ $message }}</div>@enderror
              </div>
              <div class="mb-4">
                <label class="form-label text-light small fw-bold">Password</label>
                <input type="password" name="password" class="auth-input" required autocomplete="current-password" placeholder="••••••••">
                @error('password')<div class="text-danger small mt-1">{{ $message }}</div>@enderror
              </div>
              <button type="submit" class="auth-btn">Log In</button>
            </form>
            <div class="text-center mt-3">
              <a href="#" class="text-decoration-none small fw-bold" style="color:orange;" onclick="document.getElementById('tab-register').click();return false;">Don&rsquo;t have an account? Sign Up</a>
            </div>
          </div>
          <!-- REGISTER -->
          <div class="tab-pane fade" id="content-register" role="tabpanel">
            <div class="text-center mb-4">
              <h4 class="text-light fw-bold">Create Account</h4>
              <p class="text-secondary small fw-bold">Join Baddies Club for free</p>
            </div>
            <form method="POST" action="{{ route('register') }}">
              @csrf
              <div class="mb-3">
                <label class="form-label text-light small fw-bold">Full Name</label>
                <input type="text" name="name" class="auth-input" required autocomplete="name" value="{{ old('name') }}" placeholder="Your name">
                @error('name')<div class="text-danger small mt-1">{{ $message }}</div>@enderror
              </div>
              <div class="mb-3">
                <label class="form-label text-light small fw-bold">Email Address</label>
                <input type="email" name="email" class="auth-input" required autocomplete="email" value="{{ old('email') }}" placeholder="your@email.com">
                @error('email')<div class="text-danger small mt-1">{{ $message }}</div>@enderror
              </div>
              <div class="mb-3">
                <label class="form-label text-light small fw-bold">Password</label>
                <input type="password" name="password" class="auth-input" required autocomplete="new-password" placeholder="••••••••">
                @error('password')<div class="text-danger small mt-1">{{ $message }}</div>@enderror
              </div>
              <div class="mb-4">
                <label class="form-label text-light small fw-bold">Confirm Password</label>
                <input type="password" name="password_confirmation" class="auth-input" required autocomplete="new-password" placeholder="••••••••">
              </div>
              <button type="submit" class="auth-btn">Create Account</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<x-footer />

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  function openLightbox(id, src, title, views, date, element) {
    var lb    = document.getElementById('vd-lightbox');
    var video = document.getElementById('vd-lightbox-video');
    document.getElementById('vd-lb-title').textContent = title;
    var viewsEl = document.getElementById('vd-lb-views');
    viewsEl.textContent = views;
    document.getElementById('vd-lb-date').textContent  = date;
    video.src = src || '';
    lb.classList.add('open');
    document.body.style.overflow = 'hidden';
    if (src) { video.load(); video.play().catch(function(){}); }

    fetch('/videos/' + id + '/view', {
      method: 'POST',
      headers: {
        'X-CSRF-TOKEN': '{{ csrf_token() }}',
        'Accept': 'application/json'
      }
    })
    .then(r => r.json())
    .then(data => {
       if (data.success) {
           viewsEl.textContent = data.views + ' views';
           if (element) {
               element.querySelector('.vd-views-count').innerHTML = '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="me-1"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>' + data.views + ' views';
           }
       }
    })
    .catch(console.error);
  }
  function closeLightbox() {
    var video = document.getElementById('vd-lightbox-video');
    video.pause(); video.src = '';
    document.getElementById('vd-lightbox').classList.remove('open');
    document.body.style.overflow = '';
  }
  function closeLightboxOnBackdrop(e) {
    if (e.target === document.getElementById('vd-lightbox')) closeLightbox();
  }
  document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeLightbox(); });

  var authModalEl = document.getElementById('authModal');
  if (authModalEl) {
    authModalEl.addEventListener('show.bs.modal', function(event) {
      var tab = event.relatedTarget ? event.relatedTarget.getAttribute('data-auth-tab') : null;
      var el  = document.getElementById(tab === 'register' ? 'tab-register' : 'tab-login');
      if (el) new bootstrap.Tab(el).show();
    });
  }
</script>

<!-- -- AUTH MODAL -- -->
<x-auth-modal />
</body>
</html>
